
import { Routes } from '@angular/router';
import { ItemComponent } from './item/item.component'; // Adjust the path as necessary

export const routes: Routes = [
  { path: '', component: ItemComponent }, // Default route
  // Add more routes as needed
];